

var campuses = [];
var days = [];
var classes = [];
module.exports.setClasses = function(campus, days, classes){
    this.campuses = schedule[2].split(',');
    this.days = schedule[1].split(',');
    this.classes = schedule[0].split(',');
  //buildCombos(this.campuses, this.days, this.classes);
}

var buildCombos = function(campus, day, classes){
}